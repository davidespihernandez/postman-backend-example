$kind: http-request
description: Returns a single item by its ID.
url: "{{baseUrl}}/items/:id"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: "6778"
    description: The unique integer ID of the item.
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer",
            "default": "<integer>"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
examples: ./.resources/Get item detail.resources/examples
order: 1000
